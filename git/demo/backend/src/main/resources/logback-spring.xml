<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Pattern für das Log-Layout -->
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Appender für die Haupt-Logdatei -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- täglich neue Datei + 30 Tage aufbewahren -->
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Appender nur für TwitchService-Logs -->
    <appender name="TWITCH_SERVICE_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/twitch-service.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/twitch-service.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="DISCORD_COMMAND_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/discord-commands.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/discord-commands.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="DISCORD_CONTROLLER_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/discord-controller.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/discord-controller.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <appender name="TWITCH_COMMAND_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/twitch-commands.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/twitch-commands.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root-Logger schreibt in FILE -->
    <root level="DEBUG">
        <appender-ref ref="FILE"/>
    </root>

    <appender name="TWITCH_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="streamer_website.demo.service" level="DEBUG" additivity="false">
        <appender-ref ref="TWITCH_SERVICE_FILE"/>
    </logger>

    <logger name="streamer_website.demo.client.TwitchBot" level="DEBUG" additivity="false">
        <appender-ref ref="TWITCH_COMMAND_FILE"/>
    </logger>

    <logger name="streamer_website.demo.client.DiscordBot" level="DEBUG" additivity="false">
        <appender-ref ref="DISCORD_COMMAND_FILE"/>
    </logger>

    <logger name="streamer_website.demo.controller.discord" level="DEBUG" additivity="false">
        <appender-ref ref="DISCORD_CONTROLLER_FILE"/>
    </logger>

</configuration>
