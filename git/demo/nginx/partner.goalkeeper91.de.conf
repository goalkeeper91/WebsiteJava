# partner.goalkeeper91.de - ALTE Domain, jetzt reiner 301-Redirect auf
# partner.fuchshilft.de. Zertifikat bleibt bestehen, damit der Redirect
# selbst sauber funktioniert.

server {
    listen 80;
    listen [::]:80;
    server_name partner.goalkeeper91.de;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://partner.fuchshilft.de$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name partner.goalkeeper91.de;

    ssl_certificate /etc/nginx/certs/live/partner.goalkeeper91.de/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/live/partner.goalkeeper91.de/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    location / {
        return 301 https://partner.fuchshilft.de$request_uri;
    }
}
